# Database (Neon - Vercel PostgreSQL)
# IMPORTANT: Use the POOLED connection string for best performance
# Get it from: https://console.neon.tech → Connection Details → Toggle "Pooled connection"
# The endpoint should have "-pooler" in the URL and include "pgbouncer=true"
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/bakery_hub?sslmode=require&pgbouncer=true&connect_timeout=10"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Allowed Users (comma-separated emails)
ALLOWED_EMAILS="owner@example.com,manager@example.com"

# SMS Notifications (Twilio)
# Get credentials from: https://console.twilio.com
TWILIO_ACCOUNT_SID="your_account_sid_here"
TWILIO_AUTH_TOKEN="your_auth_token_here"
TWILIO_PHONE_NUMBER="+1234567890"
SMS_NOTIFICATIONS_ENABLED="false"

# Cron Job Security
# Generate with: openssl rand -base64 32
CRON_SECRET="generate_a_secure_random_string_here"
